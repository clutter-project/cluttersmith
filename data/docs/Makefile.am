
docsdir = $(pkgdatadir)/docs
docs_DATA = $(wildcard $(srcdir)/*.json) \
            $(wildcard $(srcdir)/*.js) \
            $(wildcard $(srcdir)/*.prejs) \
            $(wildcard $(srcdir)/*.jpg) \
            $(wildcard $(srcdir)/*.png) 

EXTRA_DIST = $(docs_DATA) 
